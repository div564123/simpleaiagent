{
  "name": "votre-projet",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "echo \"No build needed\""  // Ajoutez votre commande de build ici
  },
  "dependencies": {
    "dotenv": "^version",
    "express": "^version",
    "node-fetch": "^version"
  }
}
